# AutoBuff 监控模式实现方案

## 目标

在现有“死花模式、活花模式、跟补模式”之后新增第四个模式：

> **监控模式**

监控模式只读取游戏窗口画面并显示地图信息，不执行任何键盘或鼠标操作。它提供现有“地图标注”入口，以及一个持续刷新的实时小地图。实时小地图支持三种显示模式：

1. **纯小地图模式**：只显示游戏内的小地图画面。
2. **小地图 + 标线标点模式**：显示小地图画面，并叠加当前地图已保存的平台、绳索、传送点和玩家位置。
3. **纯标线标点模式**：隐藏游戏小地图背景，只在深色画布上显示平台、绳索、传送点和玩家位置。

本阶段针对 macOS 端 `AutoBuff` 实现。Windows 端 `open-flower` 目前没有同等的地图库、地图特征匹配和标注模型，不在本次施工范围内。

## 安全边界

监控模式必须保持完全只读：

- 不启动 `LiveFlowerWorker`、`DeadFlowerWorker` 或 `FollowHealWorker`。
- 不调用 `HumanInput`，不发送方向键、技能键、鼠标点击或窗口聚焦操作。
- 不要求辅助功能权限，只要求：
  - 已选择有效的游戏窗口；
  - 已授予屏幕录制权限。
- 监控运行期间暂停独立的“自动同意组队”输入 Worker；停止监控或离开监控模式后，再按用户原有开关状态恢复，避免“监控模式只显示”与后台点击行为冲突。
- 退出监控、切换窗口、关闭应用或捕获任务取消时，必须立即停止刷新任务，不遗留后台循环。

## 当前代码基础

现有实现已经具备以下可复用能力：

- `MinimapMonitor`
  - 自动识别小地图纯内容区；
  - 截取小地图；
  - 识别自己的玩家黄点；
  - 识别一个或多个同队队友橙点；
  - 识别一个或多个其他玩家红点。
- `MapTopology`
  - 保存平台、绳索、传送点及其归一化坐标；
  - 保存地图参考图和视觉特征。
- `MinimapVisualMatcher`
  - 将当前小地图与已保存地图库进行匹配；
  - 已规避玩家、传送点等移动或高饱和标记对地图匹配的主要干扰。
- `MapTopologyLibraryView`
  - 创建和管理多张地图。
- `MapTopologyEditorView`
  - 已有平台、绳索、传送点和玩家黄点的绘制样式。

因此监控模式只需要增加“持续捕获 + 地图匹配 + 展示层”，不需要复制地图检测、标注存储或自动寻路逻辑。

## 界面设计

### 1. 模式选择区

模式顺序固定为：

1. 死花模式
2. 活花模式
3. 跟补模式
4. 监控模式

第四张模式卡内容：

- 标题：`监控模式`
- 图标：`map.circle.fill`
- 副标题：`只读显示实时地图`
- 无障碍标识：`mode.monitor`

现有三张卡横向排列在 520px 左右宽度下已经接近上限。加入第四张后改成 **2×2 网格**，保留每张卡的可读宽度，不把标题和副标题压得过窄。

### 2. 监控模式专属内容

选择监控模式后，不显示以下输入控制配置：

- BUFF 列表；
- 技能键；
- 跳跃键；
- 椅子键；
- 移动方式；
- 跟补基准点；
- 自动同意组队开关。

改为显示一个独立的“地图监控”卡片：

- 当前游戏窗口；
- 当前地图状态：
  - `正在识别`
  - `已匹配：地图名`
  - `未匹配到已标注地图`
  - `小地图识别失败`
- “管理地图”按钮，继续打开现有 `MapTopologyLibraryView`；
- 三段式显示切换器：
  - `纯小地图`
  - `小地图 + 标注`
  - `纯标注`
- 固定比例的实时预览区域；
- 帧状态文字，例如 `实时 · 30 FPS`、`玩家位置已识别` 或错误摘要。
- 实时坐标诊断：
  - 当前玩家位置，例如 `X 128.4 · Y 63.2`；
  - 当前同队队友数量，例如 `队友 2`；
  - 当前其他玩家数量，例如 `红点 3`；
  - 当前小地图可用范围，例如 `范围 X 0–201 · Y 0–126`；
  - 未识别到玩家时保留范围显示，并将坐标显示为 `X -- · Y --`。

坐标以自动识别后的小地图纯内容区为基准，左上角是 `(0, 0)`，X 向右增加，Y 向下增加。这里显示的是检测算法直接使用的像素坐标，不是屏幕全局坐标，也不是地图标注保存时的归一化坐标。这样后续开发区域判断、触发范围和位置阈值时，可以直接观察实际数值。

地图管理在尚未开始监控时可用；监控运行期间先禁用编辑，避免地图编辑窗口和监控任务同时竞争同一个游戏窗口捕获。

### 3. 底部主按钮

监控模式下底部按钮文案改为：

- 未运行：`开始监控`
- 运行中：`停止监控`

其他三个模式继续使用：

- `开始运行`
- `停止运行`

### 4. 三种显示效果

#### 纯小地图

- 显示 `MinimapMonitor.captureMinimap()` 返回的实时画面。
- 不额外绘制平台、绳索、传送点或重新绘制玩家点。
- 即使当前地图没有标注或匹配失败，也能正常使用。

#### 小地图 + 标注

- 底层显示实时小地图。
- 上层按归一化坐标叠加：
  - 平台：绿色折线；
  - 绳索：橙色竖线；
  - 传送点：蓝色圆点；
  - 玩家当前位置：放大的黄色定位点、黑色外圈，并在黄点正上方显示一个带黑色描边的向下箭头。
  - 同队队友：支持同时识别多个橙点，绘制紧凑橙色圆标和高对比描边。
  - 其他玩家：支持同时识别多个红点，绘制紧凑红色圆标和高对比描边，不添加文字标签。
- 标注编号沿用编辑器中的 `P1 / R1 / T1`。
- 当前地图未匹配时保留小地图，并在画布上方显示“未匹配标注”，不错误套用其他地图数据。

向下箭头只占用黄点上方的小块区域，箭头尖端指向黄点，不再显示 `ME` 文字，避免遮挡附近平台、绳索或传送点。纯小地图模式继续保留游戏原始画面，不额外绘制箭头。

#### 纯标线标点

- 使用深色画布，不显示实时小地图背景。
- 绘制内容、颜色、编号和比例与“小地图 + 标注”一致。
- 自己的黄点、队友橙点和其他玩家红点都是实时变化的标记；平台、绳索和传送点来自已匹配的 `MapTopology`。
- 当前地图未匹配时显示深色画布、实时玩家标记和顶部提示，不保留上一张地图的旧静态标注。

实时玩家标记不依赖地图匹配结果。只要小地图捕获和颜色检测正常，即使当前地图尚未匹配，“小地图 + 标注”和“纯标注”模式仍显示自己的黄点箭头、队友橙点及其他玩家红点。

三类玩家颜色使用互不重叠的 OpenCV HSV 色相区间：

- 其他玩家红点：`H <= 12` 或 `H >= 168`；
- 同队队友橙点：`12 < H < 20`；
- 自己的黄色点：`20 <= H <= 40`。

三类检测都继续校验饱和度、亮度、红绿蓝通道关系和连通区域形状，避免仅凭色相把暗色背景或细长 UI 当作玩家。

## 数据模型

### `AppMode`

新增：

```swift
case monitor = "monitor"
```

标题为 `监控模式`。旧配置不包含该值时继续按现有迁移逻辑加载，不影响原有三种模式。

### `MonitorDisplayMode`

新增可持久化枚举：

```swift
enum MonitorDisplayMode: String, Codable, CaseIterable {
    case minimapOnly = "minimap_only"
    case minimapWithAnnotations = "minimap_with_annotations"
    case annotationsOnly = "annotations_only"
}
```

在 `AppSettings` 中新增：

```swift
var monitorDisplayMode: MonitorDisplayMode = .minimapWithAnnotations
```

默认选择“小地图 + 标注”，旧配置缺少字段时使用该默认值。

第一版不增加刷新率设置，避免把实现变成调参界面。实时画面的目标刷新频率调整为约 30 FPS。监控不再为每帧调用一次 `SCScreenshotManager`，而是为已识别的小地图区域启动一个 `SCStream` 连续捕获会话：

- `minimumFrameInterval` 设为 `1 / 30` 秒；
- `sourceRect` 直接限制为小地图区域，避免每帧转换完整游戏窗口后再裁剪；
- 输出尺寸使用小地图的点尺寸，保持地图标注坐标与现有数据一致；
- `queueDepth` 保持较小，只允许有限的 ScreenCaptureKit 缓冲；
- 应用层只保留最新一帧。分析忙碌时到达的新帧覆盖尚未处理的旧帧，不排队回放过期画面。

捕获、分析和地图识别采用不同频率：

- 小地图捕获与界面画面：目标 30 FPS；
- 自己的黄点、队友橙点和其他玩家红点识别：跟随已取出的最新帧，目标最高 30 FPS；
- 地图视觉特征匹配：约每秒 5–6 次，避免每帧遍历地图库；
- 游戏窗口有效性：约每秒检查一次。

`SCStream` 输出回调不直接更新 SwiftUI，也不执行地图匹配。回调仅完成像素缓冲区到小地图 `ImageBuffer` 的转换并提交到“最新帧槽”；`MonitoringSession` 串行消费最新帧、执行玩家识别和低频地图匹配，再回到主线程发布结果。停止监控、切换窗口或重新识别区域时必须先停止旧流并丢弃槽内旧帧。

2026-07-19 刷新率优化验证：

- 固定的每帧 `200ms` 等待和实时阶段的逐帧截图均已移除。
- `SCStream` 配置为 `1 / 30` 秒最小帧间隔，输出范围直接限制为小地图。
- ScreenCaptureKit 队列深度为 3，应用层帧通道容量为 1；消费者落后时旧帧会被最新帧覆盖。
- 游戏窗口有效性调整为每 30 个已处理帧检查一次；窗口失效仍会由周期检查或流错误及时发现。
- 30 FPS 调度、低频工作、最新帧覆盖、流配置、带行填充的 BGRA 转换、坐标格式化、瞬时失配保留、明确新地图立即切换、玩家向下箭头几何，以及黄/橙/红玩家标记分流和噪声过滤十项针对性测试通过。
- 应用和测试目标编译成功；正常 Debug 构建使用 `AutoBuff Local Development` 签名。
- 实际 FPS 仍取决于游戏画面更新频率、像素转换与分析耗时和机器负载，应通过监控面板的实时 FPS 在游戏窗口下复核。

## 代码结构

### 1. `MonitoringSession`

新增只读捕获会话，职责是：

1. 绑定游戏窗口。
2. 首次启动时通过单帧截图自动识别小地图内容区。
3. 为识别到的区域启动目标 30 FPS 的 `SCStream` 连续捕获。
4. 以“只处理最新帧”的方式从当前帧识别自己的黄点、多个队友橙点和多个其他玩家红点。
5. 计算当前帧地图视觉特征，与 `settings.mapTopologies` 匹配。
6. 向界面发布：
   - 当前帧；
   - 玩家位置；
   - 队友位置数组；
   - 其他玩家位置数组；
   - 已匹配地图；
   - 小地图区域；
   - 状态或错误信息。
7. 捕获失败时有限重试并重新识别小地图区域，不启动任何输入恢复动作。

`MonitoringSession` 不持有 `HumanInput`，也不依赖任何寻路 Executor。这样从代码结构上保证它只能观察，不能控制。

### 2. `MapTopologyOverlayView`

将 `MapTopologyEditorView` 中纯绘制部分抽成可复用的只读叠加层：

- 平台、绳索、传送点绘制；
- `P / R / T` 编号；
- 玩家当前位置；
- 归一化坐标到当前画布尺寸的换算。

编辑手势、撤销、路径测试和自动执行仍留在编辑器中。监控页面只能使用只读绘制接口，不能触发编辑或寻路。

### 3. `MonitoringPanelView`

新增监控模式专属 SwiftUI 视图：

- 显示模式切换器；
- 地图匹配状态；
- 实时画面；
- 地图管理入口；
- 空状态、加载状态和错误状态。

显示模式切换只改变展示层，不重启捕获会话，因此切换时画面应连续。

### 4. `MainViewModel`

增加监控状态：

- 当前实时帧；
- 当前玩家位置；
- 当前匹配地图；
- 监控状态文本；
- `MonitoringSession` 生命周期。

启动逻辑按模式拆分权限要求：

| 模式 | 游戏窗口 | 屏幕录制 | 辅助功能 | 输入 Worker |
| --- | --- | --- | --- | --- |
| 活花 | 必须 | 依现有行为 | 必须 | `LiveFlowerWorker` |
| 死花 | 必须 | 必须 | 必须 | `DeadFlowerWorker` |
| 跟补 | 必须 | 必须 | 必须 | `FollowHealWorker` |
| 监控 | 必须 | 必须 | **不需要** | **无** |

停止逻辑统一取消监控会话并清理实时画面状态。模式切换、游戏窗口切换和应用退出也调用同一套停止逻辑。

### 5. `SettingsSectionView` / `ContentView`

- `ContentView` 增加第四张模式卡，并将选择区改为 2×2。
- 监控模式时用 `MonitoringPanelView` 替换普通按键配置区。
- 顶部“地图标注”入口继续保留。
- 监控模式不展示会导致输入行为的配置控件。
- 底部按钮按模式显示“运行”或“监控”文案。

## 地图匹配策略

1. 启动后识别当前小地图纯内容区。
2. 捕获纯内容帧并生成视觉特征。
3. 在 `mapTopologies` 中查找第一个通过 `MinimapVisualMatcher` 的地图。
4. 匹配成功后显示地图名并加载其标注。
5. 后续帧继续低频复核地图身份，不需要每帧都遍历地图库：
   - 玩家位置每帧更新；
   - 地图身份约每 6 个已处理帧复核一次；
   - 当前地图再次匹配成功时重置失配计数；
   - 单次或少量 `nil` 结果视为跳跃、玩家标记遮挡等瞬时波动，继续保留当前地图；
   - 连续 5 次复核都没有匹配结果时才清除当前地图，按约 5 次/秒的复核频率计算，防抖窗口约为 1 秒；
   - 如果明确匹配到另一张已保存地图，则不等待失配计数，立即切换到新地图，避免在真实换图后保留旧标注。
6. 不提供“猜测最接近地图”的降级逻辑，避免在错误地图上显示误导性标线。

## 异常处理

- **未识别到小地图**：显示原因，后台有限重试；不回退截取游戏窗口左上角的任意区域。
- **未匹配地图**：
  - 纯小地图仍正常显示；
  - 两种标注模式显示“未匹配到已标注地图”；
  - 提供“管理地图”入口。
- **玩家黄点暂时丢失**：保留地图和静态标注，隐藏玩家点并显示提示；不保留过久的旧玩家位置。
- **队友橙点变化**：每帧以当前检测结果为准；队友离开小地图后立即移除。
- **其他玩家红点变化**：每帧以当前检测结果为准；红点消失后立即移除，不保留幽灵位置。
- **游戏窗口关闭或失效**：停止监控，清空旧帧和旧标注，提示重新识别窗口。
- **屏幕录制权限失效**：停止监控并提示重新授权。
- **地图切换**：明确匹配到另一张地图时立即切换；只有无候选结果才使用约 1 秒的瞬时失配防抖。
- **捕获或分析速度较慢**：丢弃被更新帧覆盖的旧帧，只处理最新画面，不并发堆积分析任务。

## 测试计划

### 单元测试

- `AppMode.monitor` 可编码、解码和持久化。
- 旧设置缺少 `monitorDisplayMode` 时默认加载为“小地图 + 标注”。
- 三种 `MonitorDisplayMode` 均可往返保存。
- 监控模式在所有 BUFF 均关闭时不产生配置错误。
- 监控模式不校验技能键、跳跃键、椅子键或跟补基准点。
- 模式权限需求测试：监控只要求屏幕录制，不要求辅助功能。
- 单次和少量瞬时失配不会清空当前地图，连续 5 次失配后会清空。
- 明确匹配到另一张地图时会立即切换，不受失配防抖窗口影响。
- 归一化平台、绳索、传送点和玩家点能正确映射到不同尺寸画布。
- 玩家叠加标记不显示文字，使用位于黄点正上方、尖端朝下的紧凑箭头。
- 多个紧凑红色连通区域能分别识别为其他玩家；细长红色 UI、过大红色装饰和低饱和暗红噪声会被过滤。
- 多个紧凑橙色连通区域能分别识别为同队队友，并且不会同时落入自己的黄色点或其他玩家红点结果。
- 其他玩家红点能在未匹配地图时继续显示，并与自己的黄色箭头使用不同视觉样式。
- 会话停止后不再发布新帧。
- 30 FPS 配置生成正确的 `minimumFrameInterval`、小地图输出尺寸和有限 `queueDepth`。
- 最新帧槽在消费者较慢时覆盖旧帧，不按到达顺序堆积延迟。
- 捕获帧编号驱动地图匹配约每 5–6 帧一次，不影响 30 FPS 画面更新。
- 玩家坐标按一位小数显示，未识别时显示占位符；坐标范围使用 `0...(宽度 - 1)` 和 `0...(高度 - 1)`。

### 代码边界测试

- `MonitoringSession` 不引用 `HumanInput`。
- 监控模式分支不启动三个输入 Worker。
- 监控模式运行期间独立输入 Worker 处于停止状态。

### 手动验证

1. 四张模式卡按 2×2 正常排列，监控模式位于跟补模式之后。
2. 未授权辅助功能、已授权屏幕录制时，监控模式仍可启动。
3. 未启用任何 BUFF 时，监控模式仍可启动。
4. 三种显示模式可在运行中即时切换，捕获不中断。
5. 已标注地图能自动匹配并正确叠加平台、绳索和传送点。
6. 玩家移动时黄色玩家点实时更新。
7. 切换地图后不会残留上一张地图的标注。
8. 未标注地图在纯小地图模式下仍能正常显示。
9. 监控期间按键和鼠标均无自动操作。
10. 停止监控、关闭应用或游戏窗口失效后，截图任务立即结束。
11. 原有死花、活花、跟补模式行为不变。

## 推荐施工顺序

1. 增加 `AppMode.monitor`、`MonitorDisplayMode` 和设置迁移测试。
2. 抽取只读 `MapTopologyOverlayView`，先保证编辑器显示不回退。
3. 实现无输入依赖的 `MonitoringSession`。
4. 实现 `MonitoringPanelView` 和三种显示模式。
5. 修改 `MainViewModel` 的权限、启动、停止及后台输入隔离逻辑。
6. 修改模式卡布局和底部按钮文案。
7. 补充单元测试并执行针对性 Xcode 测试。
8. 完整构建 AutoBuff。
9. 按手动验证清单检查三种显示效果和“绝不输入”的边界。

## 完成标准

- [x] 监控模式显示在跟补模式之后。
- [x] 监控模式不要求辅助功能权限。
- [x] 监控模式不发送任何键盘或鼠标输入。
- [x] 可管理现有地图标注。
- [x] 可持续显示实时小地图。
- [x] 可切换纯小地图、小地图 + 标注、纯标注三种显示模式。
- [x] 已标注地图可自动匹配并显示正确标注。
- [x] 玩家位置可实时更新。
- [x] 地图切换、窗口失效和权限失效不会残留旧画面或旧标注。
- [ ] 原有三个模式通过回归验证。

## 施工验证记录

2026-07-17 已完成代码施工并验证：

- 监控模式相关新增测试全部通过：
  - 无 BUFF、无输入键配置时允许启动；
  - 只要求屏幕录制、不要求辅助功能；
  - 监控模式及三种显示方式可持久化；
  - 旧设置默认迁移到“小地图 + 标注”；
  - 地图匹配不会复用错误地图；
  - 玩家坐标可正确映射到不同尺寸画布。
- `xcodebuild build` 完整 Debug 构建成功。
- `git diff --check` 通过。
- 全量 `AutoBuffTests` 中，全部监控模式新增测试通过；当前工作区另有一个非监控相关的寻路测试
  `pathPlannerPrefersRopeWhenHangingNearButNotOnPlatform` 失败，因此“原有三个模式通过回归验证”暂不勾选。
- 仍需连接实际游戏窗口，按“手动验证”章节检查三种实时显示效果、地图切换和玩家黄点刷新。

## 地图数据持久化保护

2026-07-17 针对旧版程序覆盖设置文件造成地图标注丢失的问题，增加以下保护：

- `settings.json` 使用 `schemaVersion = 2`，只保存通用设置；地图数据不再以它为唯一存储。
- 地图标注独立保存到 `maps.json`，使用独立的地图文档版本号。
- 首次加载旧设置时，如果地图仍内嵌在 `settings.json` 中，会自动迁移到 `maps.json`。
- 新版程序始终优先读取 `maps.json`。旧版程序即使重写 `settings.json`，也不会清空独立地图库。
- `settings.json` 和 `maps.json` 每次有效变更前都会轮换保存最多 5 份备份：
  `*.bak.1` 到 `*.bak.5`。
- 主文件无法解码时，会先保存带时间戳的 `*.corrupt-*` 原始副本，再尝试从最新有效备份恢复。
- 如果没有有效备份，或者文件来自当前程序不支持的更高版本，对应文件会进入写保护，禁止用默认值或旧格式覆盖。
- 恢复、迁移和写保护信息会显示在应用日志中。

保护边界：这套逻辑能防止后续旧版覆盖、异常 JSON 和版本降级造成再次丢失；已经被旧版覆盖且本机没有备份的历史地图数据无法由代码反向恢复。

持久化保护已通过自动化用例验证：

- 地图与通用设置分文件保存，旧版只重写 `settings.json` 后地图仍能加载。
- 旧内嵌地图能迁移到 `maps.json`。
- `settings.json`、`maps.json` 损坏时均能从轮换备份恢复。
- 没有备份的损坏文件保持原内容并进入写保护。
- 即使调用方未先执行加载，也不会直接覆盖无法解码的 `maps.json`。
- 更高版本的设置文件不会被当前版本降级覆盖。
- Debug 构建成功，`git diff --check` 通过。

## 地图批量导入导出

地图库增加“批量导入”和“批量导出”入口，采用以下规则：

- 导出前显示全部地图清单，每张地图都可以单独勾选或取消，也支持全选和全不选。
- 多张已选地图导出到同一个 JSON 文件，文件包含格式版本、导出时间和完整地图数据。
- 导入先选择一个 JSON 文件，再显示文件内的地图清单；每张地图同样可以单独勾选或取消。
- 导入清单会标明平台、绳索、传送点数量，并提示哪些地图与当前地图库同名。
- 已勾选的同名地图替换当前版本，新地图追加到地图库末尾；未勾选地图不产生任何修改。
- 同名判断不区分大小写，替换时保留当前地图库的名称写法，并同步修正导入地图之间的传送目标名称。
- 导入完成后立即通过现有 `SettingsManager` 保存，因此仍受独立 `maps.json`、轮换备份和损坏写保护保护。
- 导入文件拒绝空名称、无效尺寸、重复名称和当前版本无法识别的更高格式版本。
- 为方便迁移，导入器除标准导出包外，也兼容当前 `maps.json` 文档以及旧版地图数组 JSON。

导入导出功能验证记录：

- 已选多张地图可以完整导出并往返导入，平台、绳索、传送点、连线和参考图数据均保留。
- `maps.json` 文档及旧版地图数组 JSON 均可导入。
- 同名地图能够原位替换，新地图按顺序追加，导入地图之间的传送目标名称会修正为地图库规范名称。
- 重复名称及更高格式版本会被拒绝，不修改当前地图库。
- 新增导入导出测试均通过，应用 Debug 构建与测试目标编译成功。

## 新建地图时合成纯净小地图

“根据当前小地图新建”不再直接保存单帧截图，改为短时间多帧采样：

- 自动识别小地图内容区后，在约 2–3 秒内采集 12 帧，并在界面显示采样进度。
- 黄点、橙点和红点视为会移动的玩家标记；蓝色传送点属于地图内容，不清除。
- 每帧先按 HSV 色相、饱和度和亮度识别暖色玩家标记，再向外扩张一圈，覆盖抗锯齿边缘。
- 合成每个像素时，优先使用未被玩家标记遮挡帧的通道中位数，避免移动目标留下残影。
- 如果某个位置在全部帧中都被遮挡，则根据上下或左右两侧的已恢复像素补齐；优先延续较亮的水平平台线或垂直绳索线。
- 合成后的纯净图同时用于新地图的 `referenceBGR` 和 `visualSignature`，不会把首帧中的玩家点写入地图文件。
- 第一阶段区域识别仍使用原始帧验证，避免纯净合成后因没有玩家标记而被旧校验规则错误拒绝。
- 地图新建区提供“重新抓取纯净小地图”按钮，可以在角色或队友移动后重新采样。
- 采样任务支持取消；窗口关闭后不继续更新界面或保存半成品。

纯净小地图合成验证记录：

- 移动的黄色、橙色和红色标记能够从 12 帧合成结果中完全移除。
- 蓝色传送点不会被暖色标记掩膜误删。
- 玩家标记在全部帧中停留于同一位置时，空间补齐能够恢复被遮挡的水平平台线，并保持周围暗色背景。
- 尺寸不一致的采样帧会被拒绝，不生成半成品参考图。
- 三项合成器测试全部通过，应用与测试目标编译成功。

## 需要确认的设计点

开始施工前需要确认以下默认决定：

1. 本次只实现 macOS `AutoBuff`，不同时移植到 Windows `open-flower`。
2. 实时小地图先放在 AutoBuff 主窗口的监控卡片中，不做悬浮置顶小窗。
3. 默认显示模式为“小地图 + 标注”。
4. 监控运行期间暂停“自动同意组队”，以保证整个模式完全只读。
5. 实时画面目标固定约 30 FPS，不额外增加刷新率设置；地图匹配保持约 5–6 FPS。

## 30 FPS 连续捕获施工记录

2026-07-19 确认采用以下实现边界：

- 单帧截图仅用于启动时识别小地图区域，实时阶段切换到 `SCStream`。
- 捕获区域直接设置为小地图内容区，不持续转换完整游戏窗口。
- 捕获输出队列和分析消费解耦，应用层采用“最新帧覆盖旧帧”策略。
- 不为了追上帧率并发执行多个玩家识别或地图匹配任务。
- 面板 FPS 表示实际成功发布到界面的帧率，而不是配置值。
- 面板同时显示玩家的实时小地图 X/Y 像素坐标和当前小地图坐标范围。
- 机器负载或游戏帧率导致单帧处理超过 33.3ms 时允许低于 30 FPS，但不允许延迟持续累积。

## 游戏窗口缩放兼容

2026-07-21 修复监控过程中缩放游戏窗口后提示“未匹配到已标注地图”的问题：

- 监控会定期检查目标窗口尺寸；宽或高变化超过 2 像素时，停止旧采集流并按新窗口尺寸重新识别小地图区域。
- 重新识别期间会清空旧区域、帧调度器和稳定状态，避免继续使用缩放前的裁剪坐标。
- 小地图自动搜索范围扩大，以兼容放大后的游戏窗口与小地图。
- 地图视觉签名允许缩放采样造成的一格位置偏移，同时增加亮色地图结构校验，防止容错后把线条不同的地图误判为同一地图。
- 新增窗口尺寸变化和缩放后签名匹配测试；应用 Debug 构建及测试目标编译成功。

## 地图编辑页实时相似度

2026-07-21 在“地图标注与路径连接”页面加入当前游戏小地图对比：

- 地图编辑页持续读取当前游戏小地图，并显示与正在编辑地图的实时相似度百分比。
- 同时显示是否符合监控模式的实际匹配条件、灰度差、结构差和是否使用缩放容错，避免只看百分比产生误判。
- 即使当前游戏不是正在编辑的地图，也可以查看相似度；此时继续保留保存的参考图，并禁止将实时帧用于标线或动作执行。
- 相似度计算与监控模式共用同一个视觉签名、结构校验和缩放容错实现，编辑页结果与实际匹配判断保持一致。
- 实际匹配统一采用“界面相似度不低于 60%”；结构差已经计入综合相似度，不再设置独立门槛，因此显示 60.0% 就会命中。
- 多张地图同时超过判定线时选择相似度最高者，不再按地图库顺序返回第一张候选地图。
- 旧地图缺少视觉签名时，会尝试使用保存的参考图现场生成签名。
- 相似度及缩放容错测试已补充，应用 Debug 构建及测试目标编译成功。

## 远程监看 EXP

### 模板定位 + OCR 读数

2026-07-31 将正式 EXP 识别链路从逐字符模板匹配升级为级联识别：

- 固定字体模板只匹配稳定的 `EXP` 锚点，并据此裁剪完整的经验面板。
- 裁剪后的整行交给 macOS Vision OCR，同时尝试彩色原图和高对比预处理。
- OCR 结果必须符合 `EXP + 当前经验 + 百分比` 的结构；允许括号在像素字体下被漏读，
  但仍要求百分号和合法的 `0...100` 数值。
- 正式发布由连续三帧一致确认，短时 OCR 失败继续沿用既有丢帧容忍策略。
- 原有逐字符模板识别保留为测试和诊断实现，不再进入监控模式的正式读数链路。

2026-07-26 将本机监控模式已经稳定识别的 EXP 结果接入远程纯标注协议：

- Mac 按 EXP 识别任务自身的约 500ms 节奏发送独立 `exp` 消息，不重复塞入
  每秒约 10 次的位置帧。
- 消息包含当前 EXP、经验百分比、识别置信度、识别状态和识别时间。
- 没有稳定读数时发送空数值及当前状态，网页不会继续把旧读数误认为最新结果。
- Go 服务校验数值范围，并在会话内保存最新 EXP；网页晚打开或重连后可从
  `snapshot` 立即恢复最近结果。
- 网页底部实时显示“当前 EXP”和“经验进度”，悬停可查看识别状态和置信度。
- Mac test build、Go 单元测试及 Vue TypeScript 生产构建均已通过。

## 远程推送性能与窗口稳定性

2026-07-26 根据 Xcode 现场异常栈修复监控推送期间的界面卡死：

- 实际异常来自 AppKit 的 `Update Structural Regions in Window` 循环。移除
  AppDelegate 主动 `setContentSize`、SwiftUI `.contentMinSize` 和无用途的
  `GeometryReader` 组合，改用默认窗口尺寸和系统自由缩放，避免内容刷新与
  窗口尺寸互相反馈。
- WebSocket 同一时间只允许一个发送任务；网络变慢时位置帧和 EXP 各自只
  保留最新一条，不让旧帧无限排队。
- 地图与在线状态消息使用有上限的控制消息队列，避免被位置帧覆盖。
- 远程登录 Token 和会话链接改存到
  `~/Library/Application Support/AutoBuff/remote-monitor.json`，目录权限为
  `700`、文件权限为 `600`，避免临时签名开发版反复触发钥匙串密码确认。
- 客户端启动恢复登录后先用 `/api/monitor/sessions/current` 校验本地会话；
  当前会话 ID 一致时复用原预览链接和发布地址，不再创建新会话并撤销旧链接。
- 本地没有会话记录或服务器会话已经失效时才创建新会话；从旧版升级后需要
  重新登录一次，此后应用重启不会再次生成链接。
- 修复后 Mac test build 通过，网络协议与云端服务无需再次变更。

## 符文提示识别与推送

2026-07-27 在监控模式里增加紫色「出符文」提示的识别，并接入远程推送。

### 识别算法

`RuneAlertDetector` 只看几何结构，不做模板匹配，因此窗口被任意拉伸也能命中：

- 横幅由上下两条亮紫色边框线夹住一块半透明紫色区域。检测器先逐行统计边框
  色像素占比，把连续的「热行」合并成候选线。
- 候选线要同时满足：单行边框覆盖率不低于 `0.32`、最长连续段不短于窗口宽度的
  `0.30`。
- 再在候选线里配对上下边框，要求垂直间距落在窗口高度的 `0.06 ~ 0.22` 之间、
  水平投影重叠不低于 `0.80`、夹层内部紫色像素占比不低于 `0.25`。
- 所有阈值都是相对窗口宽高的比例而非像素数，所以分辨率和长宽比变化不影响判定。
- 只用紫色本身会被界面上其他紫色元素误触发，所以最终依赖的是「两条线 + 夹层」
  这个结构；红字太稀疏、锁图标需要多尺度模板匹配，都没有采用。

`AE_RuneAlertDetectorTests` 用 9 张真实截图做正样本，另外覆盖任意拉伸、抹掉横幅
的负样本、去饱和负样本，以及四类结构条件各自的合成反例。

### 识别频率与上报

- 复用 EXP 识别任务已经在抓的整窗帧，不额外截图。EXP 约每 500ms 一帧，符文
  隔帧识别一次，即约每秒一次，由 `MonitorFrameScheduler.shouldDetectRuneAlert`
  控制。
- `RuneAlertStabilizer` 做防抖：连续 2 帧一致才改变对外状态，单帧漏检不会误报
  「已解除」。
- `RuneAlertPublishPolicy` 决定何时发 `rune` 消息：状态翻转立即发，状态不变时
  每 3 秒心跳重发一次，让服务端能判断客户端还活着。
- 监控停止或连续截图失败时，会主动把符文状态复位并上报 `false`。

### 服务端推送

- `rune` 消息包含 `detected`、`confidence`、`detectedAt`，hub 内保存会话最新符文
  状态，网页晚打开也能从 `snapshot` 恢复。
- 推送规则 `rune_alert` 有独立开关，间隔固定 5 秒：符文还挂着就每 5 秒催一次，
  符文消失后不再推，也不发「已解除」通知。
- 只有 `detected` 为真、且上报时间落在 12 秒新鲜度窗口内才会推送。客户端掉线
  后心跳停止，最后一次「有符文」的上报很快过期，不会无限推下去。
- 调度器每 5 秒扫一次，而 `last_sent_at` 记的是本轮推送完成的时刻，比 tick 晚
  几毫秒。不留容差的话每条规则都会顺延一整个周期（符文 5 秒变 10 秒、EXP 每
  分钟变 65 秒），因此 SQL 里允许提前 1 秒触发。

### 网页显示

- 预览页新增「符文诅咒」卡片，与 EXP 卡片并列，显示是否出现和识别置信度。
- 卡片下方是 `rune_alert` 的独立开关，调用
  `PUT /api/preview/notifications/rune-alert`。
- 本机离线时不显示「符文提示中」，避免把最后一次上报当成现状。
- 极简模式沿用磁盘伪装文案：状态显示为「写入锁定：已锁定 / 正常」，开关叫
  「写入锁定告警」，不暴露游戏语义。

### 图片回放测试

符文不常出现，等真实符文来验证很不方便，所以监控面板的符文卡片下方有一个
「图片测试」按钮，用本地截图走一遍完整链路。

- 只在监控运行中可用，因为远程发布通道是随监控一起建立的。
- 支持多选。读图走的是和实时截图相同的 `ImagePipeline.cgImageToBGRBuffer`，
  所以回放结果与真实运行一致。
- 结果按批汇总，例如「9 张全部识别到符文，置信度 62%–88%」；没命中的会列出
  文件名；后缀像图片但内容损坏的归入「无法读取」，与「没识别到」区分开。
- 一批图只推一次，取置信度最高的那张，避免选 9 张就收到 9 条通知。
- 命中后注入的「有符文」状态会保持 8 秒。这是必需的：实时识别每秒一帧，
  不保持的话注入状态 1 秒内就被真实画面覆盖，服务端那一轮扫描（每 5 秒）
  看不到它，推送不会触发。8 秒同时短于服务端 12 秒新鲜度窗口，测试结束后
  状态自然过期停推。保持期间实时识别结果被忽略，结束后自动复位并交还控制权。
- 监控中途停止会直接复位保持状态，此时发布通道已断开，不再补发「无符文」。
- 推送最终是否到手机还取决于网页上 `rune_alert` 开关是否打开；关着时链路会
  在服务端正常停下，不是缺陷。

### 验证

- Mac 端 `RuneAlertDetectorTests`、`RuneAlertImageTestTests`、
  `MonitorFrameSchedulerTests`、`EXPFixedFontRecognizerTests` 串行执行通过。

## 安全区越界报警

2026-07-27 新增「离开安全区报警」，同时下线了每分钟经验推送。

### 配置方式

- 基准点复用跟补模式的 `PortalMarkerView` 点击取点弹窗，但两者互不影响：
  跟补仍用自己的 `healAnchorX/Y`，监控用新的 `monitorSafeZone`。
- 基准点是矩形的**中心**，长宽向四周各扩一半。
- 长宽按**小地图百分比**调整（Stepper，2%~100%，步长 5%），不是像素。因为
  标记时监控已停止，拿不到当前内容区尺寸；而百分比本身就是存储用的值。
- 基准点贴边时矩形会被裁进小地图范围内——地图到那里就结束了，而且服务端
  要求上报的矩形不能越出 0~1。

### 为什么存归一化坐标

小地图内容区尺寸会随游戏窗口变化，`monitorContentSize` 每帧都可能变。跟补的
`healAnchorX/Y` 存的是绝对像素，换分辨率就失效。安全区改用 `NormalizedMapPoint`
加归一化长宽，和地图标注、远程推送的做法一致，换窗口大小不用重设。
`MonitorSafeZoneTests` 里有一个用例专门在三种分辨率下验证同一相对位置的判定结果一致。

### 判定与上报

- 判定跟着位置帧走，每帧比一次：把黄点像素换算成归一化坐标，再和矩形比。
  边界上算「在内」，避免站在线上反复报警又取消。
- `SafeZoneStabilizer` 做防抖，用**持续时间**而不是帧数：离开需连续 1.5 秒、
  回归需连续 1 秒。按帧计数在掉帧时会被拉长成十几秒。

### 报警必须有明确依据

报警的唯一依据是「连续 1.5 秒明确看到黄点在框外」。黄点没识别到时传 `nil`，
分三种情况处理：

- **还没报警**：不报警。找不到角色不等于角色跑了，遮挡、切场景、小地图重新
  识别都会让黄点消失。
- **越界计时进行中**：清零。刚出框 1 秒黄点就丢了，这一次不会被「补上」，
  重新看到黄点才重新起算。
- **已经在报警**：进入 10 秒宽限期。短暂丢失不中断报警（否则抖一下就漏报）；
  连续丢失满 10 秒则收回判断、停止报警，重新看到角色后重新判定。角色死亡或
  被传送后黄点会消失，此时继续按最后一次判断无限报警是没有依据的。

宽限期必须短于服务端 12 秒的新鲜度窗口：客户端每 3 秒心跳，服务端数据永远
不会自己过期，所以只能由客户端主动收回。同时要明显长于 1.5 秒的越界确认时长，
否则刚报警就容易被一次丢帧撤销。

`SafeZoneStateChange` 区分 `returned`（角色真的回来了）和 `lostTrack`
（我们不再有依据）：两者都停止报警，但日志文案不能混为一谈。
- `MonitorStatePublishPolicy`（符文和安全区共用）决定何时发 `zone` 消息：
  状态翻转立即发，状态不变时每 3 秒心跳重发。
- 没配置安全区时也会发一条不带矩形的「未越界」，让网页停止画框。

### 服务端与网页

- 推送规则 `zone_breach`，独立开关，间隔固定 5 秒：在区外就每 5 秒催一次，
  回到区内停推，不发「已回归」通知。同样有 12 秒新鲜度窗口防掉线空推。
- 监控面板的小地图叠加层会画出矩形框，画在最底层不遮挡玩家和地图标注；
  在区内是绿色虚线，越界后变红。
- 网页预览页新增「安全区」卡片和开关，并在标注画布上同样画出矩形框。
  极简模式伪装为「分区越界」状态和「分区越界告警」开关，框改为黑色虚线。

### 监控面板不要产生固有宽度压力

2026-07-27 安全区上线后再次出现 AppKit 的
`Update Structural Regions in Window` 循环异常（窗口 520×848，正好是
`MainWindowLayout.minimumContentWidth`）。原因是符文图片测试那一行里写了：

```swift
Text(runeTestHintText)
    .lineLimit(2)
    .fixedSize(horizontal: false, vertical: true)   // ← 触发点
```

`fixedSize(vertical: true)` 表示「按我的理想高度来」，而文字的理想高度取决于
分到多宽；它和同行的按钮、`Spacer` 争宽度就形成了宽高互相依赖。测试结果文案
里还可能带一串文件名，单行理想宽度远超窗口，于是每一轮都想把窗口撑宽。

改成：结果文案单独一行、用 `.frame(maxWidth: .infinity, alignment: .leading)`
接受自上而下的宽度，配合 `lineLimit(2)` 和 `truncationMode(.tail)` 截断。
步进器的标签也给了固定宽度，百分比多一位数字不会改变整行的固有宽度。

在这个面板里加控件时的约束：

- 不要对会换行的文字用 `fixedSize(vertical: true)`。
- 不要让同一行里既有固有宽度很大的元素又有 `Spacer` 去争宽度。
- 面板本身在 `ContentView` 的 `ScrollView` 里（`ContentView.swift:194`），
  所以内容变高是安全的，会被滚动吸收；**只有宽度**会顶到窗口。

### 侧栏切换不能直接修改 NSWindow

2026-07-28 继续排查到主窗口的第二个布局反馈源：`toggleSidebar()` 在修改
`sidebarExpanded` 触发 SwiftUI 布局的同一次事件里，又连续调用
`NSWindow.setContentSize` 和带动画的 `setFrame`。主窗口同时使用
`.windowResizability(.automatic)`，SwiftUI 与 AppKit 因而反复争夺内容尺寸，
最终抛出 `Update Constraints in Window` 次数超限异常。

侧栏切换现在只更新 `sidebarExpanded`。窗口尺寸保持不变，由右侧工作区吸收
侧栏宽度变化；任何视图事件都不再直接同步修改主窗口 content size 或 frame。

随后现场异常从 `Update Constraints` 变成了 `Update Structural Regions`。
剩余原因是侧栏宽度仍有 0.22 秒隐式动画，而紧凑/展开状态会同时替换多组按钮、
帮助提示和可访问区域。动画的每一帧都会让 AppKit 重建窗口交互区域，实时监控
刷新又会触发新一轮布局，最终超过系统允许的更新次数。

侧栏现在改为一次性切换，不再对这类结构变化做宽度动画。安全区 `Stepper` 也
移除了不必要的 `.fixedSize()`，只保留标签的稳定宽度，避免控制器的固有尺寸
参与窗口宽度协商。

### 每分钟经验推送下线

- 后端删除 `exp_minute` 规则、调度、`SetEXPMinuteEnabled` 与
  `PUT /api/preview/notifications/exp-minute`；`SaveBark` 不再接收该开关。
- 经验无增长提醒（`exp_stalled`）保留不变，EXP 数据通道也保留。
- 残留规则行由根目录 `zone_breach_migration.sql` 清理。
- Go 端 `go build` / `go vet` / `go test ./...` 全部通过。
- 前端 `vue-tsc` 类型检查与 Vite 生产构建通过。
- 全量跑 `AutoBuffTests` 时另有 4 个与符文无关的既有失败：两个 UI 自动化用例
  断言的是死花模式界面，而本机保存的模式是 `monitor`，启动即不匹配；
  `pathPlannerPrefersRopeWhenHangingNearButNotOnPlatform` 上文已记录；
  `followHealInitialDetectionDoesNotBiasTowardDestinationAnchor` 的 `paint`
  辅助函数用参数名遮蔽了外层图宽，索引算错，属于用例自身缺陷。

# 神殿模式开发进度

最后更新：2026-08-02

## 总体规划

神殿模式按功能拆分为独立 Worker，避免把休息室、挂绳组队和进出自由的状态机混在一起。入口统一由 `MainViewModel` 启停，设置统一存入 `AppSettings`。

| 功能 | 状态 | 当前实现 |
| --- | --- | --- |
| 休息室 | 已实现，待游戏内实测 | 监听黄点/橙点人数变化，人数增加后放 BUFF、发消息，定时左右移动 |
| 挂绳组队 | 第一阶段已实现，待联调 | 角色名、网页编队、远程切模式、首次建队邀请和入队回执 |
| 进出自由 | 已实现 | 复用死花模式 Worker，并使用独立的神殿模式显示名称 |

## 休息室：已实现

- 自动识别并持续截取游戏窗口左上角小地图。
- 每秒统计一次小地图中的黄点候选数和橙点数。
- 连续两帧数量一致后才确认变化，降低偶发丢帧造成的误触发。
- 首次人数识别只建立基线，不会因基线人数额外触发第二轮 BUFF。
- 点击开始运行后会立即释放一轮 BUFF，不等待人数变化。
- 黄点或橙点任意一种数量增加都会触发；减少会更新基线，因此 `3 → 2 → 3` 仍会触发第二次增加。
- 自动接受队伍邀请成功后会额外触发一轮 BUFF；事件进入休息室 Worker 队列，避免和正在执行的释放流程交错按键。
- 只在确认黄点或橙点数量增加时释放 BUFF；BUFF 倒计时结束不会触发释放。
- 人数增加触发后释放所有已启用且已配置按键的 BUFF，不检查持续时间和剩余倒计时。
- 每个 BUFF 沿用既有拟人操作：按两次，双击间隔 0.1–0.3 秒；不同 BUFF 间隔 2–3 秒；按键按住时间沿用全局 50–150 毫秒。
- BUFF 全部处理后先执行：回车、输入 `/队伍`、回车发送；再重新唤醒聊天并发送一条随机完成喊话。
- 喊话使用加权随机池：10 条常规队伍文案占 85%，5 条短句占 15%；不会连续两次选择相同的基础文案。
- 喊话结尾随机使用无标点（70%）、句号（20%）或 `~`（10%）。
- 聊天字符按 50–120 毫秒随机间隔输入，唤醒聊天、发送前和两条消息之间均有较短随机停顿。
- 防卡动作按随机间隔执行：短暂向右，再短暂向左。
- 防卡间隔可配置最短和最长分钟数，默认 15–30 分钟，允许 1–1440 分钟。
- 游戏窗口失效时自动停止；截图短暂失败时保留运行并重试。
- 启停、人数变化、BUFF、聊天和防卡动作均写入运行日志。

## 休息室：待验证与后续优化

- 在实际游戏聊天框中验证 macOS Unicode 事件能稳定输入中文 `释放完毕`。若游戏客户端拒绝 Unicode 事件，需改为剪贴板粘贴或游戏输入法方案。
- 采集不同分辨率、缩放比例和神殿休息室背景下的黄点/橙点样本，复核颜色阈值和两帧确认策略。
- 验证组队、退队、传送和小地图短暂隐藏时的数量变化是否符合预期。
- 根据实测决定是否把识别频率和“连续确认帧数”开放为高级配置。
- 根据实测微调防卡移动按住时长（当前每个方向 120–260 毫秒）。
- 可选：在界面显示当前已确认的黄点/橙点数量和下次防卡移动时间。

## 挂绳组队：第一阶段已实现

- 客户端可填写并保存游戏角色名称，也可打开或复制客户端管理网页链接。
- 客户端管理网页的每张客户端卡片都可填写并保存角色名称。
- 网页支持“创建队伍/修改队伍”对话框，只显示已填写角色名的客户端，最多选择 5 个并单选一名队长。
- 保存时要求所有所选客户端在线，防止队伍配置已保存但远程指令无法执行。
- 队伍配置、队长、成员和入队状态持久化在服务端数据库。
- 保存后每个成员客户端按顺序执行：停止当前模式 → 切换神殿模式/挂绳组队 → 开启自动同意组队 → 开始运行。
- 第一次创建队伍时，队长依次发送 `/退出隊伍`、`/建立隊伍`，再按成员角色名逐条发送 `/邀请组队 xxx`。
- 每条聊天指令都会重新按回车唤醒聊天框，拟人输入后再按回车发送；指令之间带随机停顿。
- 网页可移除非队长成员；队长客户端会拟人发送 `/踢出隊伍 角色名`，无需停止正在运行的挂绳模式。
- 修改已有队伍并点击“保存并执行”时，会按当前成员重新建队和发送邀请，网页状态由客户端执行回执实时推进。
- 队员自动接受邀请后通过设备 WebSocket 上报入队成功，网页实时显示“队长/已进队/等待进队”。
- 队伍创建后可在网页设置目标老板名称；任一客户端 Buff 进入最后 10 秒会启动单一老板 Buff 周期。
- 队长每 8 秒拟人发送 `/邀請組隊 老板名`，并以连续两帧橙点数量变化确认老板进队。
- 老板进队后服务端广播所有队伍客户端强制释放全部已勾选 Buff，并按周期汇总完成回执。
- 全部客户端完成后，服务端通知队长发送 `/踢出隊伍 老板名`；成功回执后周期回到等待状态。
- 服务端升级脚本为 `monitor-backend/migrations/rope_party_migration.sql`。

### Windows 客户端补齐情况

- Windows 客户端已增加神殿模式入口和“挂绳组队”选择，并持久化角色名、队伍编号、队长身份及邀请名单。
- 已增加角色名保存、“客户端管理”和“复制链接”操作；角色名通过统一后端接口保存。
- 已支持网页下发 `configure_rope_party`：停止当前模式、切换神殿/挂绳、开启自动同意组队并开始运行。
- 首次创建队伍时，Windows 队长也会按顺序拟人输入 `/退出隊伍`、`/建立隊伍` 和各成员邀请命令；中文使用 Windows Unicode 键盘事件逐字输入。
- Windows 成员成功点击接受邀请后，会发送 `team_joined` 回执，网页可同步更新入队标记。
- 每个账号只保存一个队伍；网页支持解散，在线队长收到指令后拟人输入 `/退出隊伍`，并清除本地队伍状态与自动同意组队。
- Windows 已补齐休息室完整状态机：首次释放、两帧人数确认、减少后再增加、自动接受组队触发、队伍频道与随机喊话、可配置防卡移动。
- Windows 已把进出自由接入现有死花 Worker，沿用进出市场、移动、BUFF 和返回流程；界面可直接配置同一套选项。

## 挂绳组队：留待下次继续

- 实现角色自动走到绳索、上绳、定位和掉绳恢复。
- 增加入队超时、邀请失败、客户端中途离线和队长失败的重试流程。
- 明确挂绳后的 BUFF、喊话、移动和回位行为。
- 完成多客户端、真实游戏和生产数据库迁移联调。

## 进出自由：已实现与待整理

- 已复用 `DeadFlowerWorker` 完成现有运行流程。
- 已使用“神殿模式 · 进出自由”作为日志显示名称。
- 待后续确认神殿地图是否需要独立的市场识别、传送门或路线参数；若行为开始分叉，应拆出独立 Worker，避免长期依赖死花模式细节。

## 测试记录

- 已增加人数追踪单元测试，覆盖：两帧确认、减少后重新增加、单帧丢点不改变基线。
- Windows 已增加休息室人数追踪、多人黄点计数、防卡间隔持久化、挂绳指令顺序、远程配置协议及入队回执测试；Windows 测试共 72 项通过，23 项因当前非 Windows/PyQt 环境按预期跳过。
- 已通过 Swift 全源码静态类型检查。
- 生产后端已于 2026-08-02 完成 `role_name`、`rope_teams`、`rope_team_members` 幂等迁移并重启；本机端口与 Nginx 公网入口健康检查均通过。
- 当前环境只有 Command Line Tools，无法执行 Xcode scheme 构建与 XCTest；需要在安装完整 Xcode 的环境执行：

```bash
xcodebuild -project AutoBuff.xcodeproj -scheme AutoBuff -configuration Debug test
```

## 关键文件

- `AutoBuff/Workers/LoungeWorker.swift`：休息室状态机与自动操作。
- `AutoBuff/Workers/RopePartyWorker.swift`：首次建队与邀请指令执行。
- `open-flower/workers/rope_party_worker.py`：Windows 首次建队与邀请指令执行。
- `open-flower/workers/lounge_worker.py`：Windows 休息室状态机、喊话与防卡移动。
- `open-flower/utils/lounge.py`：Windows 人数变化追踪和随机喊话纯逻辑。
- `open-flower/utils/remote_monitor_client.py`：Windows 远程编队和入队回执协议。
- `AutoBuff/Models/AppSettings.swift`：神殿功能选择及防卡间隔持久化。
- `AutoBuff/Views/SettingsSectionView.swift`：休息室设置界面。
- `AutoBuff/ViewModels/MainViewModel.swift`：Worker 启停、日志和配置校验。
- `AutoBuff/Services/KeyboardUtils.swift`：聊天文本的 Unicode 键盘事件。
- `AutoBuffTests/LoungePopulationTrackerTests.swift`：人数变化规则测试。

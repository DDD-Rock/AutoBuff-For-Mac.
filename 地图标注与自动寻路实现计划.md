# 地图标注与自动寻路实现计划

## 目标

把冒险岛小地图转换成可保存、可编辑、可执行的路径图，最终供自动寻路脚本计算角色从当前位置到目标位置的移动步骤。

实施分为两个阶段：第一阶段建立可靠的静态地图数据；第二阶段补充角色能够执行的跨平台连接，并生成路径图。

## 第一阶段：平台、绳索、传送点标注和保存

### 地图管理

- [x] 从游戏窗口正确截取纯小地图内容区，不包含外部白框。
- [x] 根据当前小地图创建地图，并在创建时定义地图名称。
- [x] 保存小地图参考图和视觉指纹。
- [x] 当前小地图匹配已有地图时禁止重复创建。
- [x] 支持保存和管理多张地图。
- [x] 支持选择已创建的地图并进入修改页面。
- [x] 旧版单地图设置自动迁移为多地图库。
- [x] 地图识别忽略玩家黄点、其他玩家红点和传送门等动态标记。

### 平台标注

- [x] 支持手工绘制平台线段。
- [x] 支持根据玩家黄点自动采集平台。
- [x] 采集时由用户手动控制角色左右移动，程序只持续读取小地图。
- [x] 合并左右往返的重复轨迹，生成一条平台折线。
- [x] 保留斜坡和不完全水平的平台形状。
- [x] 每完成一次自动采集立即保存，关闭修改窗口不会丢失。

### 绳索标注

- [x] 支持手工绘制绳索。
- [x] 选择“绳索”工具后，自动采集切换为绳索模式。
- [x] 根据玩家上下攀爬的黄点轨迹生成一条绳索。
- [x] 合并上下往返的重复轨迹。
- [x] 每完成一次自动采集立即保存。

### 传送点标注

- [x] 在每张地图的数据中增加传送点模型。
- [x] 地图编辑器持续刷新当前小地图，并支持通过按钮按角色实时黄点位置标注传送点。
- [x] 支持设置传送点类型，例如普通传送门、地图出口、特殊入口或当前地图传送。
- [x] 支持配置传送点连接的目标地图和目标传送点，包括同图双向传送与多点循环传送。
- [x] 支持修改、删除和立即保存传送点。
- [x] 校验传送点是否位于平台或绳索可到达范围内。

### 第一阶段完成标准

- [x] 一张地图的平台、绳索和传送点均可完整保存并重新加载。
- [x] 动态玩家标记变化不会导致同一张地图被识别成新地图。
- [x] 关闭应用并重新打开后，所有地图标注保持一致。
- [x] 标注数据具有版本号，后续增加路径连接时可以兼容迁移。

> 当前状态：第一阶段已完成，下一步进入第二阶段，先建立平台、绳索和传送点组成的只读路径图。

## 第二阶段：跳跃/下落连接与可执行路径图

### 连接数据模型

- [x] 为路径图定义节点：平台端点、平台关键折点、绳索端点、传送点。
- [x] 定义路径边的基础类型：步行、爬绳、接近、跳跃、下落、传送。
- [x] 根据平台标注生成双向步行边。
- [x] 根据绳索标注生成双向爬绳边，并连接附近的平台节点。
- [x] 根据传送点配置生成有方向的同图或跨地图传送边。
- [x] 取消平台间向上跳跃边；向上移动统一通过附近绳索完成。
- [x] 根据平台几何关系自动生成单向下落边，无需手工标注。
- [x] 自动下落边包含方向、起点、落点、按键时长和落点容差。
- [x] 区分双向步行/爬绳连接与单向跳跃/下落连接。
- [x] 为跳跃/下落连接预留验证状态和成功/失败统计。

### 跳抓绳连接

- [x] 根据绳索底部与附近平台的位置生成“平台到绳索”单向连接。
- [x] 跳抓绳优先选择绳索左下方预备点，路径先步行到预备点，再执行已验证的右跳抓绳组合；左侧不可用时才镜像到右侧。
- [x] 绳索沿高度拆分为中间攀爬节点；平台只能连接到横向距离合适且向上高度差不超过角色跳跃能力的绳索节点。
- [x] 保存朝绳方向以及跳跃键、方向键的不同松开延迟。
- [x] 保证跳起后跳跃键和方向键快速但不在同一毫秒松开。
- [x] 自动执行“朝绳方向 → 跳跃 → 上”的拟人错时组合，起跳前固定方向，随后错时松开跳跃键和方向键。
- [x] 根据黄点是否进入绳索范围验证跳抓结果；失败统计持久化待实现。

### 下落连接

- [x] 自动生成“下 + 跳跃”以及从平台左右边缘走出的下落方式。
- [x] 同一 X 方向只连接下方首先接住角色的平台，不穿过中间平台。
- [x] 最下层平台不生成继续向下的连接。
- [x] 垂直下跳路径穿过绳索时禁止生成原地下跳边，避免角色抓绳卡住。
- [x] 生成下落起点、方向和目标平台信息。
- [x] 支持执行原地下跳和左右边缘走出下落，并根据黄点确认目标平台。
- [x] 原地下跳使用随机的准备停顿、下键提前量和跳跃按压；跳跃触发后先松下键、再错时松开跳跃键，避免途中抓绳下爬。
- [ ] 根据实际失败统计自动修正或禁用错误的下落连接。

### 绳索与平台连接

- [x] 根据距离确认绳索顶部、底部连接的附近平台。
- [x] 生成并执行“平台到绳索”和“绳索到平台”的边。
- [x] 以容差处理绳索端点与平台的少量坐标偏差。
- [x] 支持从平台跳抓绳索的特殊连接及拟人化错时松键参数。

### 路径图生成与校验

- [x] 从地图标注生成统一的图结构。
- [x] 检查与主路径图不连通的孤立平台、绳索和传送点。
- [x] 在地图编辑器的路径预览中显示路线方向和动作类型。
- [ ] 支持禁用错误的自动连接并重新验证。
- [ ] 保存生成后的路径图，并在地图标注变化后局部重建。

### 自动寻路执行

- [x] 根据角色黄点判断当前所在平台或绳索。
- [x] 支持选择目标平台或传送点。
- [x] 使用带动作代价的最短路径搜索计算动作序列。
- [x] 支持执行路径开头连续的平台步行边，并根据黄点确认每一步到达。
- [x] 平台步行持续按住方向键直到到达目标；起步反应、位置观察、松键稳定等待和走过头后的回头停顿均在范围内随机。
- [x] 步行执行支持超时、偏离平台停止、连续丢失黄点停止和紧急释放方向键。
- [x] 支持执行路径开头的下落边，并在落地后重新定位和规划。
- [x] 支持一次连续执行当前规划中的步行、下落、跳抓绳、上下爬绳、离绳落台和传送动作。
- [x] 在地图编辑页提供独立的动作测试窗口，可分别触发左右步行、进入/上下爬绳、平台下跳/边缘下落、左右跳抓绳、左右离绳和传送按键组合，并可紧急停止释放按键。
- [ ] 在游戏中逐项人工验证动作测试窗口内的全部动作组合。
- [ ] 每个动作执行后重新识别角色位置，确认成功后再继续。
- [ ] 动作失败时有限次数重试；仍失败则重新规划，而不是无限重复按键。
- [ ] 角色偏离预期路径时能够重新定位到路径图。
- [ ] 增加紧急停止、超时和输入释放保护。

### 第二阶段完成标准

- [ ] 能从任意已标注平台规划到同一地图内的目标平台。
- [ ] 路径可以组合步行、绳索、跳跃和下落动作。
- [ ] 能通过传送点继续规划到另一张已标注地图。
- [ ] 每一步均以实时黄点位置确认结果，失败时能够安全停止或重新规划。
- [ ] 不存在持续按键、按键未释放或无上限重试。

## 推荐施工顺序

1. 完成第一阶段的传送点数据模型、编辑和保存。
2. [x] 建立平台、绳索和传送点组成的只读路径图。
3. 实现平台内步行和绳索连接。
4. 实现下落连接采集与执行。
5. 实现跳跃连接采集与执行。
6. 加入路径搜索、逐步执行和失败重规划。
7. 最后接入跨地图传送点寻路。

## 更新规则

- 每完成一个功能并通过验证后，将对应项目从 `- [ ]` 改为 `- [x]`。
- 不以“代码已编写”作为完成标准；需要能够保存、重新加载并通过实际运行或测试验证后才打勾。
- 如果数据结构或施工顺序发生变化，应同步修改本文件，避免实现与计划脱节。

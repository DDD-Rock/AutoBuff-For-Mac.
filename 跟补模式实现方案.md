# 跟补模式实现方案

## 目标

在 macOS 端 AutoBuff 中，在“死花模式”“活花模式”之后加入第三种运行模式：“跟补模式”。

跟补模式的核心行为是：

- 复用现有 BUFF 配置：启用状态、技能键、持续时间、倒计时显示、最多 8 个槽位。
- 不显示/不使用活花、死花下面的移动方式、跳跃键、坐椅子、出市场移动等模式参数。
- 新增“加血技能键”配置。
- 新增“标记点位”按钮，运行前在小地图上手动标记跟补基准点。
- 运行时使用手动标记点的 X 坐标作为基准点，不再在点击开始后自动识别当前黄点 X 作为基准。
- 运行期间持续释放加血技能。
- BUFF 到期时，加血暂停，优先释放到期 BUFF；BUFF 放完后继续加血。
- 每隔随机 `12-15s` 做一次轻微站位修正：向手动基准 X 中心小走一段；如果当前就在中心附近，则向右小走一步。
- 如果角色被怪物碰撞或手动走出基准 X 区域，持续按住对应方向键走回区域内；只校正 X，不管 Y。
- 所有按键保持拟人化的按压时长、间隔和随机行为。

## 当前代码现状

相关 Mac 端代码位置：

- `AutoBuff/AutoBuff/ViewModels/MainViewModel.swift`
  - 当前 `AppMode` 只有 `deadFlower`、`liveFlower`。
  - 当前模式持久化依赖 `settings.returnToMarket` 这个 Bool，第三种模式不能继续只靠它表达。
  - Worker 编排目前只有 `LiveFlowerWorker` 和 `DeadFlowerWorker`。
- `AutoBuff/AutoBuff/Views/ContentView.swift`
  - 模式选择区当前只有两个按钮。
- `AutoBuff/AutoBuff/Views/SettingsSectionView.swift`
  - BUFF 配置下面根据 `viewModel.mode` 切换活花/死花参数。
- `AutoBuff/AutoBuff/Workers/LiveFlowerWorker.swift`
  - 已有 BUFF 独立倒计时、到期释放、按两下技能、释放前后移动的逻辑。
- `AutoBuff/AutoBuff/Workers/DeadFlowerWorker.swift`
  - 已有小地图黄点检测、焦点恢复、左右移动、BUFF 批量释放、按两下 BUFF、倒计时更新逻辑。
- `AutoBuff/AutoBuff/Detection/MinimapMonitor.swift`
  - 已支持自动识别小地图深色区域和查找玩家黄点。
- `AutoBuff/AutoBuff/Services/HumanInput.swift`
  - 已有人类化方向键、鼠标点击、普通按键、按下/松开按键封装。
- `AutoBuff/AutoBuff/Services/KeyboardUtils.swift`
  - 普通技能键已有随机 50-150ms 按压时长。

因此跟补模式应新增独立 Worker，但复用现有设置、倒计时、小地图和输入基础设施。

## 数据模型设计

### 1. 新增运行模式持久化

当前 `returnToMarket: Bool` 只能区分死花/活花。第三种模式建议新增可编码枚举：

```swift
enum AppMode: String, Codable, CaseIterable {
    case deadFlower = "dead"
    case liveFlower = "live"
    case followHeal = "follow_heal"
}
```

然后在 `AppSettings` 中新增：

```swift
var mode: AppMode = .deadFlower
var healSkillKey: String = ""
var healAnchorX: Int? = nil
var healAnchorY: Int? = nil
```

兼容策略：

- 保留旧字段 `returnToMarket`，避免破坏旧 `settings.json`。
- `SettingsManager` 读取旧配置时，如果没有 `mode` 字段，则用 `returnToMarket == true` 映射为死花，否则映射为活花。
- 保存时同时写入 `mode`，并继续维护 `returnToMarket = (mode == .deadFlower)`，让旧逻辑和文档过渡期更安全。

### 2. 加血技能键

`healSkillKey` 放在 `AppSettings`，通过虚拟键盘选择，与 BUFF 键、跳跃键、椅子键一致。

校验规则：

- 跟补模式必须至少有一个可用 BUFF，规则与前两种模式一致。
- 跟补模式必须设置加血技能键。
- 跟补模式必须先手动标记基准点。
- 跟补模式需要辅助功能权限和屏幕录制权限，因为要持续读小地图黄点。

## UI 设计

### 1. 模式选择区

`ContentView.modeSelector` 加第三个模式按钮：

- 标题：`跟补模式`
- 副标题：`自动补血并回到基准点`
- 图标建议：`cross.case.fill` 或 `heart.circle.fill`
- `accessibilityIdentifier`：`mode.follow_heal`

当前窗口宽度是 520 左右，三个卡片横排可能偏挤。建议仍沿用现有卡片风格，但将按钮改为三列紧凑布局，必要时缩短副标题字号或在窄宽度下换行。

### 2. 设置区

`SettingsSectionView` 继续先显示 BUFF 配置。

BUFF 配置下面根据模式切换：

- 活花：保持当前活花选项。
- 死花：保持当前死花选项。
- 跟补：显示单独的跟补选项。

跟补选项只包含：

- `加血技能键`：按钮显示当前 `healSkillKey`，为空时显示 `选键`。
- `基准点`：按钮打开小地图标记界面，确认后保存 `healAnchorX/Y` 和当时的小地图区域，运行时只使用 X 坐标作为基准。

不显示：

- 移动方式
- 提前释放
- 出市场后移动方式
- 跳跃键
- 空闲时坐椅子
- 传送门标记

`KeyboardTarget` 新增：

```swift
case healSkillKey
```

`applySelectedKey` 中写入 `settings.healSkillKey`。

## Worker 设计

新增文件：

```text
AutoBuff/AutoBuff/Workers/FollowHealWorker.swift
```

职责：

- 管理跟补模式运行循环。
- 管理 BUFF 到期和倒计时。
- 管理持续加血。
- 监控小地图黄点 X 坐标，定时向基准 X 中心轻微修正站位。
- 走出启动基准 X 区域时执行回位。
- 确保停止时释放所有按键。

### 启动流程

1. `MainViewModel.startWorker()` 在 `mode == .followHeal` 时：
   - 校验窗口有效。
   - 校验辅助功能权限。
   - 校验屏幕录制权限。
   - 校验 BUFF 配置。
   - 校验 `healSkillKey` 不为空。
   - 调用 `followHealWorker.start(settings: settings, windowID: window.windowID)`。
2. Worker 启动后：
   - 绑定 `MinimapMonitor` 到当前窗口。
   - 将游戏窗口置前。
   - 优先复用标记基准点时保存的小地图区域，避免启动后重新阻塞识别。
   - 读取设置中的 `healAnchorX` 作为 `baseX`。
   - 记录日志：`使用手动跟补基准点 X=...`。
   - 如果旧配置没有保存小地图区域，则先进入 BUFF/补血节奏，再在需要定位黄点时兜底识别小地图。
3. 初始 BUFF 释放：
   - 与现有活花/死花保持一致，未建立倒计时时，所有启用 BUFF 视为立即到期。
   - 开始后先释放一轮启用 BUFF，再进入持续加血循环。

### 主循环优先级

每轮循环按以下优先级执行：

1. 窗口有效性和焦点检查。
2. 是否有 BUFF 到期或即将到期。
3. 是否已经走出启动基准 X 区域，需要回位。
4. 是否到达下一次轻微站位修正时间。
5. 释放加血技能。

BUFF 优先级最高。只要 BUFF 到期：

- 先停止方向移动。
- 暂停加血。
- 释放到期 BUFF。
- 更新倒计时。
- 等待 BUFF 后摇。
- 继续回位/加血循环。

### BUFF 释放

复用现有策略：

- 每个 BUFF 按两下。
- 两次之间随机间隔 `100-300ms`。
- 多个 BUFF 之间随机间隔 `250-650ms`，跟补模式里补血优先级很高，BUFF 插队时间不能拖太长。
- 倒计时从第二次按键成功时间开始计算。
- 支持把 10 秒内即将到期的 BUFF 合并释放，减少频繁打断补血。

额外注意：

- BUFF 释放后不能立刻判定完成，需要等待后摇。
- 建议 BUFF 批量释放结束后等待 `1.0-1.6s`，比加血后摇明显更长。

### 加血释放

加血技能后摇较短，建议做成随机行为组合，比固定连点更像真人：

模式 A：急促连按

- 连按 `2-4` 次。
- 单次按压 `45-120ms`。
- 两次之间间隔 `60-180ms`。
- 一组结束后等待 `120-350ms`。

模式 B：短按住

- 按住 `180-420ms`。
- 松开后等待 `120-300ms`。

模式 C：长按住

- 按住 `650-1400ms`。
- 松开后等待 `160-360ms`。
- 长按期间仍要允许 BUFF 到期打断；实现时不要一次睡死太久，建议按 `100-150ms` 小片段检查取消和 BUFF 到期。

每轮随机选择：

- 25% 使用急促连按。
- 20% 使用短按住。
- 55% 使用长按住。

实现上建议在 `HumanInput` 中新增专用方法，而不是在 Worker 中直接发 CGEvent：

```swift
func tapNamedKey(_ key: String, holdMS: ClosedRange<Int>) throws -> TimeInterval
func holdNamedKey(_ key: String, holdMS: ClosedRange<Int>) throws -> TimeInterval
```

这样 BUFF、加血和后续其它模式都能共享拟人化按键能力。

### 轻微站位修正和回基准区域逻辑

只看小地图黄点 X 坐标：

- `baseX`：用户在小地图上手动标记的基准点 X。
- `currentX`：循环中实时黄点 X。
- `delta = currentX - baseX`。
- 基准区域建议使用手动标记 X 左右 `6.0` 小地图点。

站位修正规则：

- 启动后先进入补血循环，不立刻强制修正站位。
- 之后每隔随机 `12-15s` 做一次轻微站位修正。
- 如果 `currentX > baseX`，向左小走一段，靠近标记 X 中心。
- 如果 `currentX < baseX`，向右小走一段，靠近标记 X 中心。
- 如果已经在中心附近，即 `abs(currentX - baseX) <= 3.0`，向右小走一步。
- 单次修正方向键默认按住 `200-300ms`，可在界面设置，松开后随机等待 `220-750ms` 再继续补血。
- 这个修正是为了避免短时间原地释放技能失效，同时不再左右来回走，不主动走到其他平台。

大幅偏离回位规则：

- `delta = currentX - baseX`。
- `abs(delta) <= 6.0`：认为仍在启动基准 X 的允许区域内，不强制回位。
- `abs(delta) > 6.0`：认为被怪物碰撞或误移动带偏，需要回到基准区域内。
- 超过活动带且 `delta > 0`：角色在基准区域右侧，按住左方向回位。
- 超过活动带且 `delta < 0`：角色在基准区域左侧，按住右方向回位。

移动方式：

- 回位时不使用一点点挪动；确认方向后持续按住方向键。
- 按住期间每 `80-140ms` 读取一次小地图黄点。
- 一旦检测到回到 `baseX ± 6.0` 区域内，立刻松开方向键。
- 如果检测到越过区域另一侧，则松开并切换方向持续回位。
- 单次回位最多持续约 `5s`，超时则松开方向键等待下一轮检测，避免盲走。

焦点保护：

- 移动前确认游戏窗口在前台。
- 如果窗口失焦，先释放方向键，再尝试恢复焦点。
- 停止 Worker 时调用 `human.releaseAll()`。

### 站位修正不要走太远

- 轻微站位修正只做一次短走，不连续短按。
- 修正目标是靠近手动基准点中心，或在中心附近向右轻挪一步。
- 修正后只要仍在 `baseX ± 6.0` 区域内即可。
- 一旦走出这个区域，触发持续按住方向键回位。
- 回位使用拟人化变速按压，不使用 2-3 秒级长按，也不使用稳定频率的连续短按。

### 黄点丢失处理

小地图黄点可能被遮挡或检测失败。

建议规则：

- 连续 1-3 次丢失：停止移动，继续加血，但降低移动决策频率。
- 连续 4-10 次丢失：暂停回位，只做加血和 BUFF。
- 连续超过 10 次丢失：记录警告日志，但不强停，避免临时遮挡导致整个模式退出。
- 如果正在移动时丢失黄点，必须先 `stopMove()`。

原因：跟补模式主要目标是活着，黄点短暂丢失时继续补血比盲目走位更安全。

## MainViewModel 编排

需要新增成员：

```swift
private let followHealWorker = FollowHealWorker()
```

`wireWorkers()` 中接入：

- `onLog`
- `onCountdown`
- `onError`
- `onStopped`

`startWorker()` 中新增分支：

```swift
case .followHeal:
    followHealWorker.start(settings: settings, windowID: window.windowID)
    appendLog("跟补模式已启动")
```

`stopWorker()` 中新增：

```swift
followHealWorker.stop()
```

屏幕录制权限判断从“只死花需要”改为：

```swift
if (mode == .deadFlower || mode == .followHeal) && !screenRecordingGranted
```

传送门标记按钮只在死花显示，跟补不显示。

## 测试计划

### 单元测试

新增或调整 `AutoBuffTests`：

- 旧配置没有 `mode` 字段时，能根据 `returnToMarket` 正确迁移。
- 新配置能保存/读取 `mode == .followHeal`。
- `healSkillKey` 能保存/读取。
- 跟补模式校验：未设置加血键时不能启动。
- 回基准区域方向判断：
  - 当前 X 在基准区域右侧，应向左。
  - 当前 X 在基准区域左侧，应向右。
  - 在基准区域内，不移动。
- 跟补站位修正判定：
  - 下一次修正间隔为随机 `12-15s`。
  - 偏右时向左，偏左时向右，中心附近默认向右小走一步。
  - 轻微修正后仍以启动基准 X 作为中心。
  - 走出 `baseX ± 6.0` 小地图点时触发回位。
- BUFF 倒计时仍从第二次按键时间开始。

建议把纯计算逻辑拆成可测结构，例如：

```swift
enum FollowHealNavigation {
    static let arrivalTolerance: CGFloat = 3.0
    static func directionToBase(currentX: CGFloat, baseX: CGFloat) -> HumanInput.Direction?
    static func shouldAntiIdle(now: TimeInterval, lastMovementAt: TimeInterval) -> Bool
}
```

### 构建验证

使用现有稳定命令：

```bash
cd AutoBuff
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
xcodebuild \
  -project AutoBuff.xcodeproj \
  -scheme AutoBuff \
  -destination 'platform=macOS' \
  test -only-testing:AutoBuffTests CODE_SIGNING_ALLOWED=NO
```

如果 UI 测试仍有启动不稳定，先以 `AutoBuffTests` 作为本次功能的稳定单元测试信号。

### 手动验证

1. 启动 AutoBuff，识别游戏窗口。
2. 切到跟补模式。
3. 设置 BUFF 和加血技能键。
4. 开始运行。
5. 观察日志中是否记录基准 X。
6. 确认启动后先释放 BUFF，再持续补血。
7. 让角色被怪物碰撞或手动走出区域，确认持续按方向键回到基准区域内。
8. 观察 12～15 秒左右是否会向标记 X 中心轻微修正；如果已经在中心附近，则向右小走一步，且不会走太远。
9. 等待 BUFF 到期，确认补血暂停、BUFF 按两下、等待后摇、继续补血。
10. 停止运行，确认方向键和技能键都松开。

## 实施步骤

1. 调整模式和设置模型
   - 将 `AppMode` 移到模型层或保持可被 `AppSettings` 编码。
   - `AppSettings` 新增 `mode`、`healSkillKey`。
   - `SettingsManager` 加兼容解码逻辑。
2. 调整 UI
   - 模式选择区增加跟补模式。
   - 设置区增加 `followHealOptions`。
   - 虚拟键盘目标增加 `healSkillKey`。
3. 扩展输入工具
   - 在 `HumanInput` 增加可指定按压时长的普通键短按/按住方法。
   - 保持现有 `pressNamedKey` 行为不变，避免影响死花/活花。
4. 新增跟补导航纯逻辑
   - `FollowHealNavigation.directionToBase`
   - `FollowHealNavigation.shouldAntiIdle`
   - 相关单元测试。
5. 新增 `FollowHealWorker`
   - BUFF 到期管理。
   - 加血循环。
   - 手动基准点 X 读取。
   - 向基准 X 中心轻微修正站位。
   - 走出启动基准 X 区域时回位。
   - 焦点恢复和停止释放按键。
6. 接入 `MainViewModel`
   - Worker 生命周期。
   - 权限校验。
   - 启动参数校验。
   - 日志和倒计时绑定。
7. 更新 README
   - 增加跟补模式说明。
   - 增加配置项 `mode`、`healSkillKey`。
   - 增加运行注意事项。
8. 运行测试和构建验证。

## 需要确认的默认参数

如果你没有异议，施工时我会先按这些默认值实现：

- 回基准区域完成范围：`baseX ± 6.0` 小地图点。
- 回位方向按住：每次 `120-420ms`，按偏移量动态取值并加入随机。
- 回位观察间隔：`180-650ms`，有概率额外停顿 `0.8-1.8s`。
- 回位最多连续 `3-5` 次，之后暂停移动决策 `1.0-2.5s` 再判断。
- 站位修正间隔：每轮随机 `12-15s`。
- 站位修正按住：默认 `200-300ms`，可在界面设置。
- 基准区域范围：手动标记 X 左右 `6.0` 小地图点。
- BUFF 批量合并窗口：`10s`。
- BUFF 之间间隔：`250-650ms`。
- BUFF 后摇等待：`0.8-1.2s`。
- 加血急促连按概率：`25%`。
- 加血短按住概率：`20%`。
- 加血长按住概率：`55%`。

## 风险和边界

- 小地图黄点检测依赖屏幕录制权限、窗口比例和游戏 UI 状态；跟补模式应在黄点丢失时保守处理，避免盲走。
- “被怪物碰撞”的判断无法直接读取碰撞事件，只能通过黄点 X 偏离手动基准区域来推断。
- 跟补站位修正和回位都只按小地图 X 坐标判断；如果玩家在 Y 方向变化但 X 不变，不影响修正节奏，这符合当前需求中“不管 Y”的约束。
- 游戏窗口失焦时，所有方向键和技能键都要先释放，再恢复焦点，避免游戏残留方向输入。
- 跟补模式需要持续截图读取小地图，循环频率不应过高；建议小地图检测间隔控制在 `150-300ms`，加血按键节奏另行随机。
